spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mymoney?serverTimezone=UTC
    username: root
    password: 123456

mybatis:
  mapper-locations: classpath:mappers/*Mapper.xml

logging:
  level:
    com.tianyisoft.mymoney: debug
